<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.oms.mapper.ErpSkuImageMapper">

  <delete id="deleteBySkuIdAndPlatSrcId">
      delete
      from erp_sku_image where deleted = 0
                           and store_name = #{storeName}
                           and (sku_id, plat_src_id) IN (
      <foreach close=")" collection="list" item="item" open="(" separator="),(">
          #{item.skuId}, #{item.platSrcId}
      </foreach>
      )
  </delete>
</mapper>